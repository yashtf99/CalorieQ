# Secrets only — copy this file to .env and fill in values.
# All non-secret config (DB URL, region, model IDs, etc.) lives in config.py.

SECRET_KEY=your-jwt-secret-key-here
AWS_BEARER_TOKEN_BEDROCK=your-bedrock-token-here
